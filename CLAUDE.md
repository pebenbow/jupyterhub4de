## Agent skills

### Issue tracker

Issues are tracked as GitHub issues on `pebenbow/jupyterhub4de`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root (created lazily as decisions get made). See `docs/agents/domain.md`.
